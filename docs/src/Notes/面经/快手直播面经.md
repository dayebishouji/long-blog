---
updateTime: '2025-12-21 18:26'
tags: 面经
---
## 一面
线上接口、sql问题排查

滑动窗口限流、令牌桶限流、漏桶限流

mysql主从同步

两大数之和

```java
int l1,l2;
int carry=0;
for(int i=0; i<maxLen || carry!=0; i++ ){

  int num1= i>=l1 ? 0: s1[i]-'0';
  int num2= i>=l2 ? 0: s2[i]-'0';
  int sum=num1+num2+carry;
  sb.append(sum%10);
  carry=sum/10;
}
return sb.reverse();
```
## 二面
技术指标、业务指标

这么保证项目可靠交付？

如果订单量增长10倍，怎么保证刚刚提到的指标？

压测了么？

最大乘积子数组，不要求连续

```java
long temMax=max;
max=Math.max(nums[i],Math.max(max,  Math.max(temMax*nums[i], min*nums[i] ))
min=Math.min(nums[i],Math.min(min,  Math.min(temMax*nums[i], min*nums[i] ))
result=Math.max(result,max);
```
